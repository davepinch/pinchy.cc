<article class="bookmark-masthead">  
  {%- if include.item.thumbnail -%}
  <img class="thumbnail" src="{{ include.item.thumbnail }}">
  {%- endif -%}
  
  <h1 class="title">{{ include.item.title }}</h1>
  <section class="excerpt">
    {{ include.item.excerpt | markdownify }}
  </section>

  <a class="url" href="{{ include.item.bookmark }}">{{ include.item.bookmark }}</a>

  {%- if include.item.license -%}
  <section class="license">
    {%- if include.item.license-url -%}
    <a href="{{ include.item.license-url }}">{{ include.item.license }}</a>
    {%- else -%}
    {{ include.item.license }}
    {%- endif -%}
  </section>
  {%- endif -%}

</article>