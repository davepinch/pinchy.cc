{% assign p = include.page | default: page %}
{% assign a = include.abstract | default: false %}
<article class="article">

    <header>
        <h1>{{ p.title }}</h1>
    </header>

    <section>
        {% if a %}
            {{ p.excerpt | strip_html }}
        {% else %}
            {{ p.content }}
        {% endif %}
    </section>

    <footer>
        {{ p.date }}
    </footer>

</article>