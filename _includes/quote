{% assign p = include.page | default: page %}
{% assign v = p.quote | default: p.title %}
<article class="quote">
  <section class="text">{{ v | markdownify }}</section>
  {% if p.attribution %}
    <section class="attribution">
      {{ p.attribution | markdownify}}
    </section>
  {% endif %}
</article>
