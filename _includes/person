{% assign p = include.page | default: page -%}
{%- assign v = include.person -%}
<article class="person">
    <h1 class="name">{{ v.name }}</h1>
    <section class="bio">
        {{ v.bio }}
        {% if v.wikipedia %}
        <a href="{{ p.person.wikipedia }}">[Wikipedia]</a>
        {% endif %}
    </section>
    <img class="portrait" src="{{ p.person.thumbnail.url }}" />
</article>
