{% assign p = include.page | default: page %}
<div class="poster">
    <article class="person">
        <h1 class="name">{{ p.person.name }}</h1>
        <section class="bio">
            {{ p.person.bio }}
            {% if p.person.wikipedia %}
            <a href="{{ p.person.wikipedia }}">[Wikipedia]</a>
            {% endif %}
        </section>
        <img class="portrait" src="{{ p.person.thumbnail.url }}" />
    </article>
</div>
