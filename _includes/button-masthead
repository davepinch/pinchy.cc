<div class="button-masthead">

  {% if include.item.target -%}
  <form class="button-form" method="get" action="{{target}}">
  {%- endif %}

  <button class="button" type="submit">{{ include.item.title }}</button>

  {% if include.item.target -%}
  </form>
  {%- endif %}
  
</div>